你现在是我的研究代码助手。请基于当前代码仓库，帮助我实现论文“性能评估实验”的完整实验框架与可复现实验脚本。请先通读项目代码与目录结构，再给出实现方案，然后直接完成实现。不要臆造不存在的文件、模块或接口；如果仓库里已有相近实现，请优先复用和扩展。

【研究背景】
我的论文研究的是面向 Serverless 工作流的投机执行优化方法 SpecSW。系统是在不修改 OpenWhisk 源码的前提下、以平台外方式实现的。此前我已经完成了“核心算法仿真实验”，现在要做的是“性能评估实验”，目标是验证该方法在完整端到端工作流中的实际性能收益，而不是再次验证算法本身的预测机制。

【本次任务目标】
请在当前仓库中实现一套可复现的性能评估实验框架，用于比较以下三种执行方式在端到端工作流上的表现：
1. NoSpec：非投机执行
2. BaseSpec：基础投机执行（仅使用已有基础路径统计/静态分支预测能力，不启用输入感知与分布漂移适应）
3. SpecSW：完整方法（同时启用输入感知分支预测与分布漂移适应，即combined）

【实验对象】
请围绕以下三类端到端工作流实现或整理实验负载：

1. 请求路由与风控流
2. 事件日志分析与告警流
3. 内容感知媒体处理流

工作流应尽量体现真实风格应用处理链路，并包含顺序依赖、条件分支和结果汇总等结构。

【场景要求】
每类工作流至少支持以下两类输入场景：
A. 稳定分布场景：输入分布基本稳定
B. 阶段切换场景：运行到中途发生阶段性分布变化，用于体现 distribution drift

其中：
- 请求路由与风控流：应设计为强输入相关分支场景，后继分支明显受输入字段影响
- 内容感知媒体处理流：也应设计为强输入相关分支场景
- 事件日志分析与告警流：可设计为中等输入相关，同时更突出阶段性分布变化的影响

【并发设置】
每类工作流支持至少三档并发水平：
- low
- medium
- high

请在仓库中用统一配置方式管理这些参数，避免硬编码。

【核心指标】
请实现指标采集与汇总，至少包括：
1. 端到端平均时延
2. 中位数时延
3. P95 时延
4. P99 时延
5. 总请求数
6. 成功请求数 / 成功率
7. 总投机次数
8. 错误投机次数
9. 错误投机率
10. 每请求平均额外函数触发数
11. 总函数调用数
12. 相对 NoSpec 的时延下降比例

如果仓库中已有可稳定获取的额外指标，也可以补充，但不要为了采集指标而大改系统主逻辑。

【实验组织方式】
请实现一套统一实验入口，使我可以方便执行如下实验矩阵：
- 3 类工作流
- 2 类场景（stable / drift）
- 3 档并发（low / medium / high）
- 3 种方法（NoSpec / BaseSpec / SpecSW）
- 每组重复运行 N 次（默认 3 次，可配置）

请确保实验结果可自动输出为结构化文件，例如 CSV 或 JSON，方便后续论文绘图和统计分析。

【结果输出要求】
请至少产出以下内容：
1. 统一实验运行脚本
2. 统一配置文件
3. 指标采集与汇总代码
4. 原始结果输出（CSV 或 JSON）
5. 汇总表生成脚本
6. 绘图脚本，至少能生成：
   - 平均时延对比图
   - P95 / P99 对比图
   - 资源开销对比图（如总投机次数、错误投机率、额外触发数）
7. README 或实验说明文档，说明如何运行全部实验、如何复现实验结果、各输出文件在哪里

【实现约束】
1. 不要修改 OpenWhisk 源码
2. 不要破坏当前系统已有功能
3. 优先复用现有模块、配置和日志机制
4. 不要伪造数据；如果某些指标当前无法可靠获取，请明确指出，并给出最小侵入式实现方案
5. 文件命名、目录组织、代码风格尽量与现有仓库一致
6. 所有新增代码要尽量模块化，避免把实验逻辑散落在业务代码里

【工作步骤】
请严格按以下顺序执行：
第一步：扫描代码仓库，识别现有工作流定义、执行入口、控制器、预测器、日志采集点、实验脚本和结果输出逻辑。
第二步：先给出简洁但具体的实施方案，说明：
- 哪些现有模块会被复用
- 需要新增哪些文件
- 需要修改哪些文件
- 各项指标从哪里采集
- 如何区分 NoSpec / BaseSpec / SpecSW
第三步：直接开始实现，不要停留在只给建议。
第四步：实现完成后，给出：
- 修改/新增文件清单
- 如何运行单个实验
- 如何运行全部实验
- 输出结果示例
- 还存在的限制或待补充点

【特别注意】
这次任务的重点是“端到端性能评估框架”，不是再次做算法推导。请避免把主要精力放在单独的预测准确率分析上，而应聚焦于完整工作流中的端到端时延、尾延迟和资源开销。请优先保证实验框架可运行、结果可汇总、图表可生成。

现在先从扫描当前仓库开始，然后给出实施方案，再继续完成实现。